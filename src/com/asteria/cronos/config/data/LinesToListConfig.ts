import { StreamProcessConfig } from '../../../gaia/gaia.index';

/**
 * The <code>LinesToListConfig</code> interface represents the configuration of a <code>LinesToListProcess</code> stream
 * process.
 */
export interface LinesToListConfig extends StreamProcessConfig {}